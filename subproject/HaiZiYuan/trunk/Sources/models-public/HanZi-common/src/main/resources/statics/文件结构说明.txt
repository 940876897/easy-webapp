静态资源主要分为四大块：
    1：前台JS类库框架，统一放入jsLibs中，如是单文件JS类库，则直接放在这个目录下，不要再版本号，以便以后升级版本时，直接覆盖而不用修改代码中的引用；如果是多文件JS类库，请在这个目录下创建子目录，文件夹名称为JS类库名，不要同样不要带版本号。
    2：模块图img片目录。
    3：模块js脚本目录。
    4：模块样式css目录。
    注意，img、js、css目录按照模块进行划分，每个模块划分为三个子目录，分别对应前台专用，后台专用，以及前后台通用的子目录，命名方式如：js-模块名-前后台（如通用，则到模块名为止）。